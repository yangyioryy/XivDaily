# XivDaily arXiv 429 修复计划

## 1. 问题结论

- 当前问题不是后端启动失败，而是后端启动后立即拉起 `PaperSyncService`，
  首轮同步直接请求 arXiv，触发 `429 Too Many Requests`。
- 现有实现虽然做了单进程内的串行请求和 3.2 秒间隔控制，但仍存在几个
  关键风险：
  - Web 启动与同步任务强耦合，开发阶段频繁重启会频繁触发同步。
  - 默认每个分类抓取 `200` 条，启动首轮请求偏重。
  - `429` 后仍在同一轮内连续重试，容易持续命中限流窗口。
  - 同步缺少“上次成功同步位置”概念，每次都从 `start=0` 开始抓。
- 官方约束明确要求 legacy API 整体遵守“每 3 秒不超过 1 次请求、单连接、
  不得试图绕过限流”。因此不采用代理池、多 IP 轮换等规避策略。

## 2. 修复目标

1. 后端进程启动时不再立即直连 arXiv。
2. `/papers` 等接口优先只读本地数据库，不因 arXiv 限流导致启动阶段报错。
3. 同步任务在开发环境默认可关闭，在生产环境也具备温和退避能力。
4. 同步逻辑从“全量首页抓取”改为“可恢复、可降级、可增量”的模式。
5. 即使 arXiv 暂时返回 `429`，系统也应保持服务可用，并给出明确状态。

## 3. 最小闭环方案

### Phase 1：先止血

- 在配置层增加或明确开发环境默认值：
  - `ARXIV_SYNC_ENABLED=false` 用于本地开发默认关闭自动同步。
  - `ARXIV_SYNC_MAX_RESULTS` 默认从 `200` 下调到 `25` 或 `50`。
  - `ARXIV_SYNC_CATEGORIES` 开发期默认只保留一个分类，例如 `cs.CV`。
- 调整启动逻辑：
  - 后端启动时不再立刻执行同步。
  - 即使启用自动同步，也先等待首个周期，或单独由手动接口触发首轮同步。
- 验收标准：
  - 启动 `uvicorn` 后不再出现首轮 arXiv 请求日志。
  - `/health`、`/papers` 在无外网请求前提下可正常响应。

### Phase 2：同步任务与 Web 启动解耦

- 将“定时同步”从 `FastAPI lifespan` 中抽离，改为以下二选一或组合：
  - 独立脚本命令，例如 `python -m app.jobs.sync_arxiv`
  - 后台计划任务入口，由外部调度器按固定周期执行
- Web 服务仅负责：
  - 读取本地论文库
  - 暴露同步状态
  - 按需提供手动同步入口
- 验收标准：
  - 后端重启不会触发同步。
  - 手动执行同步命令时，数据库仍可正常写入。

### Phase 3：实现 429 友好的同步策略

- 调整 `ArxivClient`：
  - 遇到 `429` 时不在同一轮里密集重试 3 次。
  - 使用指数退避，并把失败信息返回给上层，而不是直接把异常扩散为启动期
    错误噪声。
  - 保留单连接和请求间隔控制，但将退避时间拉长到分钟级更合理。
- 调整 `PaperSyncService`：
  - 单个分类失败时记录失败原因和时间，不阻断整个服务可用性。
  - 同步失败后更新“下次最早重试时间”，避免每轮都重复触发同一错误。
- 验收标准：
  - 人工模拟 `429` 后，服务仍然保持可访问。
  - 日志中出现明确的“跳过本轮/延后重试”信息，而不是长堆栈刷屏。

### Phase 4：从全量抓取改为增量抓取

- 为同步状态增加持久化记录，建议至少保存：
  - `category`
  - `last_successful_sync_at`
  - `last_attempt_at`
  - `last_error`
  - `next_retry_after`
- 调整查询策略：
  - 优先用 `submittedDate` 时间窗收窄查询范围。
  - 只抓取自上次成功同步以来的新论文或最近窗口内的小批量论文。
  - 避免每次都固定 `start=0 + max_results=200`。
- 验收标准：
  - 第二次同步请求量明显小于首次同步。
  - 数据库中已有数据时，重复同步只增补新数据，不重复扫大页。

### Phase 5：接口降级与状态反馈

- `/papers`、`/summaries/trends` 仅基于本地库响应。
- 当本地库为空或同步未完成时：
  - 返回明确状态，例如“暂无同步数据”或“最近同步失败，显示缓存结果”。
  - 不在接口请求链路内临时直连 arXiv 兜底。
- 如有必要，新增同步状态查询接口，返回：
  - 最近成功同步时间
  - 最近失败原因
  - 当前是否允许重试
- 验收标准：
  - arXiv 不可用时，接口仍返回业务可解释结果。
  - 前端能够据此展示“暂无最新同步”而不是直接报错。

## 4. 具体改动顺序

1. 调整配置默认值，先让本地开发环境不再启动即同步。
2. 修改 `main.py`，移除或延后启动时的首轮同步。
3. 修改 `arxiv_client.py`，重做 `429` 退避与错误返回策略。
4. 修改 `paper_sync_service.py`，加入失败降级和下一次重试控制。
5. 如需要，新增同步状态模型或配置持久化文件。
6. 最后补充手动同步入口和状态接口。

## 5. 不采用的方案

- 不使用代理池、多 IP 轮换、伪装多个出口来规避 arXiv 限流。
- 不把前端接口重新改回“用户每次请求时实时打 arXiv”。
- 不通过无限增大重试次数来“碰碰运气”。

## 6. 验证方案

### 启动验证

- 启动命令：

```powershell
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

- 预期结果：
  - 服务成功启动。
  - 启动阶段不再出现 arXiv 请求日志。
  - 启动阶段不再出现 `paper_sync_service` 的 `429` 异常堆栈。

### 同步验证

- 手动触发同步后验证：
  - 请求频率符合单连接、长间隔策略。
  - 若 arXiv 返回 `429`，任务进入退避，而非持续重试。
  - 若请求成功，本地数据库数据有新增。

### 接口验证

- 检查：
  - `/health`
  - `/papers`
  - `/summaries/trends`
- 预期结果：
  - 即使同步失败，接口仍能稳定返回。
  - 返回内容体现“本地缓存/同步状态”，而不是直接报外部依赖错误。

## 7. 当前推荐执行策略

- 第一阶段只做最小止血：
  - 关闭启动即同步
  - 下调单次抓取量
  - 保留手动同步
- 第二阶段再做结构性优化：
  - 解耦同步与 Web
  - 增加同步状态持久化
  - 做增量同步
- 第三阶段再补完整体验：
  - 同步状态接口
  - 更清晰的前端提示
  - 更稳妥的日志与告警

## 8. 下一步最小闭环

下一步直接实施以下最小修改，即可先消除“每次启动后端就报 429”：

1. `main.py` 去掉启动阶段立即同步。
2. `.env` 或默认配置中关闭本地自动同步。
3. `arxiv_client.py` 将 `429` 重试改为更长退避并跳过本轮。
4. 回归验证后端启动与基础接口。
