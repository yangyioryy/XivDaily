# Harmony 前端说明

本目录是 `XivDaily` 的 Harmony 前端工程。

当前阶段先完成以下最小闭环：

- 基于 Stage 模型建立标准 Harmony 单模块工程。
- 跑通首页、收藏库、对话、设置四个主入口。
- 用本地样例状态复刻 Android 参考截图的主要布局层级。
- 关键流程先保留中文注释和状态边界，方便后续继续替换成真实接口。

后续待补内容：

- 真实网络层与后端接口适配。
- 本地偏好持久化、错误态和加载态。
- 联调验证与截图对比收口。
