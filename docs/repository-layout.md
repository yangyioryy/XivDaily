# 仓库结构说明

```text
XivDaily/
├── android/   # Android 客户端工程占位
├── backend/   # Python 后端工程占位
├── docs/      # 架构、接口、运行说明
├── issues/    # 任务状态唯一来源
├── plan/      # 计划与范围基线
└── scripts/   # 联调与辅助脚本
```

## 布局原则

- 前后端代码分目录独立演进，避免配置和依赖混放。
- 文档集中放在 `docs/`，避免说明散落到脚本或代码目录。
- 计划与 issue 状态独立管理，便于执行闭环和回溯。
